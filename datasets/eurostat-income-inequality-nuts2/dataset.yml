root: "../../out"
schema_version: 1

dataset:
  name: "eurostat_income_inequality_nuts2"
  source_id: "eurostat"
  years: [2026]
  time_coverage:
    start_year: 2003
    end_year: 2025

raw:
  output_policy: overwrite
  sources:
    - name: "eurostat_ilc_di11_r"
      type: "script"
      args:
        command: "../../connectors/tsv_normalize.py --flow ILC_DI11_R --output ilc_di11_r_normalized.parquet"
        output: "ilc_di11_r_normalized.parquet"
        filename: "ilc_di11_r_normalized.parquet"
      primary: true

clean:
  sql: "sql/clean.sql"
  read:
    source: auto
    mode: latest
    delim: ","
    encoding: utf-8
    header: true
  validate:
    min_rows: 100

mart:
  tables:
    - name: "mart_eurostat_income_inequality_nuts2"
      sql: "sql/mart.sql"
  required_tables:
    - "mart_eurostat_income_inequality_nuts2"

validation:
  fail_on_error: true

registry:
  dataflow: "ILC_DI11_R"
  theme: "Income inequality S80/S20 by NUTS 2 region"
  nuts_level: 2
  dimensions: ["freq", "unit", "geo"]
  description: "Income quintile share ratio S80/S20 by NUTS 2 region"
